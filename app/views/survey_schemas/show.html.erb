<p id="notice"><%= notice %></p>



<div class="backgroundLogin">
  <div class="page-header">
    <div class="header">
      <h1> <%=@survey_schema.title%> </h1>
    </div>
  </div>
  <div class="form1">
    <div id="table-wrapper">
      <table class="table table-striped table-hover ">
      <% contador = 0%>
        <% for i in 0..@categories.length-1 %> 
          <thead class="categoryTitle <%="cat#{i}"%>">
            <td colspan="6"><%=@categories[i]%></td>
          </thead>
          <thead>
            <tr class="primary">
              <th class="panel-title">#</th>
              <th>Nombre de pregunta</th>
              <th>Habilidad</th>
            </tr>
          </thead>
          <tbody>
            
          <% @questions_category[i].each do |question|%>
            <tr>
              <% contador +=1 %>
              <td> <%= contador %></td>
              <td><%= question.name%></td>
              <td><%= question.skill%></td>
            </tr>
            <%end%>
          </tbody>
        <%end%>
        </table>
      </div>

    <div class="btn-group btn-group-justified">
    <p>
      <a href="/survey_schemas" class="btn btn-primary">Ir a pautas de evaluación</a>
    </p>
  </div>
    </div>
</div>
