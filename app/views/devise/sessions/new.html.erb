
<div class="backgroundLogin">
  <div class="login">

  <h2>Iniciar Sesión</h2>

    <div class="form">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <%= f.email_field :email, class: "inputField", autofocus: true, placeholder: "Correo electrónico" %>
        <%= f.password_field :password, class: "inputField", autocomplete: "off", placeholder: "Contraseña" %>

      <% if devise_mapping.rememberable? -%>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end -%>

      <div class="actions">
        <%= f.submit "Iniciar Sesión", class: "submit" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
    </div>
  </div>
</div>