  <div class="page-header">
    <div class="header">
      <h1> Questions </h1>
    </div>
  </div>
  <div class="index">
    <table class="table table-striped table-hover ">
      <thead>
        <tr class="primary">
          <th class="panel-title">#</th>
          <th>Title</th>
          <th>Type of question</th>
          <th>Edit</th>
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
        <% contador = 0%>
        <% @questions.each do |question|%>
        <tr>
          <% contador +=1 %>
          <td> <%= contador %></td>
          <td><%= question.name%></td>
          <%if question.genre == "text" %>
            <td><img src="images/textbox.png" width="50%" ></img> </td>
          <%else%>
            <td><img src="images/scale.jpg" width="50%" height="40%"></img> </td>
          <% end %>
          <div class="btn-group btn-group-justified">
          <td><%= link_to 'Edit', edit_question_path(question) %> </td>
          <td> <%= link_to 'Delete', question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <%end%>
      </tbody>
    </table>

    <br>
    <div class="btn-group btn-group-justified">
      <p><a href="questions/new" class="btn btn-primary btn-lg">New question</a></p>
    </div>
  </div>
