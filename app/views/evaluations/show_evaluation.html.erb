<div class="index">
  <table class="table table-striped table-hover ">
    <thead>
      <tr class="primary">
        <th class="panel-title">#</th>
        <th>Pregunta</th>
        <th>Respuesta</th>
      </tr>
    </thead>
    <tbody>

    <form action="/evaluations" method="post" >
    <header>
    <h2 class="evaluation-header"> Evaluación del profesor <%= User.find_by(id: @evaluation.user).name %></h2>
    <header>
    <br>
    <% contador = 0%>
    <%  @evaluation.answers.each do |q| %>
    <tr>
      <% contador +=1 %>
      <td> <%= contador%></td>

      <td> <%=q.name %> </td>
      <td> <input name="answers[<%=q.id%>]" type="text" value= <%= q.text %> /> </td>
    </tr>

      <br/>
    <%end%>
    </tbody>
  <table>
    <div class="text-right">

      <a href="/evaluate" class="btn btn-primary btn-lg">Regresar</a>
    </div>
  </form>
<div>
